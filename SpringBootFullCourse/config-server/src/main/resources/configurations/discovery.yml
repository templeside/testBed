eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/	# eureka.instance.hostname =  line3 localhost
server:
  port:  8761           # same as server.port

# configserver 
spring:        
  application:
    name: discovery           